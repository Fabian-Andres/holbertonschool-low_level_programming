#include <stdio.h>
#include <stdlib.h>

/**
 * malloc_checked - function malloc_checked
 *
 * @b: Value
 * Return: Always 0.
 */
void *malloc_checked(unsigned int b)
{
	int *new_str;

	new_str = malloc(b);

	if (str == NULL)
	{
		exit(98);
	}
	return (str);
}
